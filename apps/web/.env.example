# URL API Railway — dipakai server-side di hooks.server.ts & +page.server.ts
API_URL=https://your-api.railway.app/api

# URL API Railway — dipakai client-side di browser (harus prefix PUBLIC_)
PUBLIC_API_URL=https://your-api.railway.app/api
