{
  "name": "kasir-apotik",
  "private": true,
  "scripts": {
    "dev:api": "pnpm -F @pharmapos/api dev",
    "dev:web": "pnpm -F @pharmapos/web dev",
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "lint": "pnpm -r run lint"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}
